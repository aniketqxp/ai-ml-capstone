# ── web + db ─────────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn==0.28.0
sqlalchemy==2.0.28
psycopg2-binary==2.9.9
pydantic==2.6.4
python-dotenv==1.0.1
python-multipart==0.0.9
requests==2.32.3

# ── pipeline (worker: transcribe -> segment -> acoustic -> evaluate -> export) ─
# torch is the CPU wheel on the Space via --extra-index-url in the Dockerfile
litellm==1.90.0
langgraph==1.2.6
faster-whisper==1.2.1
transformers==5.12.1
huggingface-hub==1.21.0
torch==2.12.1
soundfile==0.14.0
librosa==0.11.0
numpy==2.2.6
pandas==2.3.3
